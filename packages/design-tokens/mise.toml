[tasks]
build = { run = "node --import=tsx scripts/build.ts", description = "Build design tokens for web and mobile" }
"build:css" = { run = "node --import=tsx scripts/build-css.ts", description = "Build CSS tokens for web only" }
"build:flutter" = { run = "node --import=tsx scripts/build-forui-theme.ts", description = "Build Flutter theme for mobile only" }
dev = { run = "node --import=tsx scripts/watch.ts", description = "Watch tokens.ts for changes and rebuild" }
install = { run = "pnpm install", description = "Install dependencies" }
test = { run = "pnpm test", description = "Run design-tokens tests" }
lint = { run = "pnpm biome check --write --unsafe", description = "Run biome linter" }
format = { run = "pnpm biome check --write", description = "Run biome formatter" }
typecheck = { run = "tsc --noEmit", description = "Run TypeScript type check" }
